- title: 빠른 속도
  subtitle: 속도와 성능을 갖춰 빌드하세요.
  text: Swift는 코드의 표현력과 접근성을 유지하면서도 가장 높은 성능이 요구되는 환경을 충족합니다. Swift는 네이티브 코드로 직접 컴파일되며 예측 가능한 메모리 관리를 제공합니다.
  code: |-
    // Vectorized check that a utf8 buffer is all ASCII
    func isASCII(utf8: Span<SIMD16<UInt8>>) -> Bool {
      // combine all the code units into a single entry
      utf8.indices.reduce(into: SIMD16()) {
        // fold each set of code units into the result
        $0 |= utf8[$1]
      }
      // check that every entry is in the ASCII range
      .max() < 0x80
    }

- title: 표현력
  subtitle: 간결한 코드. 강력한 결과.
  text: Swift는 초보자도 이해할 수 있는 간결하고 읽기 쉬운 문법으로 고급 코드를 작성할 수 있게 합니다. 경험 많은 개발자에게 익숙한 객체지향, 함수형, 제네릭 프로그래밍 패턴을 지원합니다. 점진적 공개 방식을 통해 언어를 빠르게 익히고, 필요에 따라 고급 기능을 활용할 수 있습니다.
  code: |-
    import ArgumentParser

    // Complete implementation of a command line tool
    @main struct Describe: ParsableCommand {
      @Argument(help: "The values to describe.")
      var values: [Double] = []

      mutating func run() {
        values.sort()
        let total = values.reduce(0, +)

        print(
          """
          Smallest: \(values.first, default: "No value")
          Total:    \(total)
          Mean:     \(total / Double(values.count))
          """)
      }
    }

- title: 안전성
  subtitle: 메모리 안전성을 보호하세요.
  text: Swift는 안전성을 최우선으로 하며, 설계상 특정 부류의 버그와 취약점을 원천적으로 제거합니다. 메모리 안전성과 데이터 레이스 안전성은 언어의 핵심 기능으로, 코드베이스에 쉽게 통합할 수 있습니다. 안전성은 애플리케이션이 실행되기 전인 컴파일 시점에 보장됩니다.
  code: |-
    let transform = Affine2DTransformBuilder()
        .translate([10.0, 20.0].span)
        .rotate(30.0)
        .build()

    let v = [11.0, 22.0, 1.0]

    // Call C functions safely with Swift types
    let u = mat_vec_mul(
      transform, rowCount, colCount, v.span, allocator)
    let uMagnitude = vec_mag(u.span)

- title: 상호 운용성
  subtitle: 기존 코드에 점진적으로 도입하세요.
  text: Swift는 외부 함수 인터페이스 없이도 C와 C++ 타입을 네이티브로 이해하고, 양방향 접근을 위한 브릿징을 제공하여 비할 데 없는 상호 운용성을 갖추고 있습니다. Swift의 상호 운용성 기능을 활용하면 전체 코드를 다시 작성하지 않고도 기존 코드베이스에 점진적으로 도입할 수 있습니다.
  code: |-
    import CxxStdlib

    // Use types from C++, like std::string, directly
    let beverages: [std.string] = [
      "apple juice", "grape juice", "green tea"
    ]

    let juices = beverages.filter { cppstring in
      // and call methods directly on C++ types
      cppstring.find(.init("juice")) != std.string.npos
    }

- title: 적응성
  subtitle: 마이크로컨트롤러에서 서버까지.
  text: "마이크로컨트롤러에서 커널, 서버에서 앱까지 아우를 수 있는 유일한 언어입니다. Swift는 바이트 하나하나가 중요한 펌웨어 같은 제약된 환경부터 하루에 수십억 건의 요청을 처리하는 클라우드 서비스까지, 어디서든 탁월한 성능을 발휘합니다."
  code: |-
    // Configure UART by direct register manipulation 
    // using Swift MMIO. Enables RX and TX, and sets
    // baud rate to 115,200. Compiles down to an
    // optimal assembly sequence with no overhead.

    usart1.brr.modify { rw in
      rw.raw.brr_field = 16_000_000 / 115_200
    }

    usart1.cr1.modify { rw in
      rw.ue = .Enabled
      rw.re = .Enabled
      rw.te = .Enabled
    }

- title: 오픈 소스
  subtitle: 기여하고 참여하세요.
  links:
    - link: https://github.com/swiftlang
      text: GitHub에서 SwiftLang 보기
    - link: https://forums.swift.org
      text: 포럼 참여하기
